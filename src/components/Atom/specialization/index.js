// components/Atom/specialization/index.js

const Specialization = ({hashtags}) => {
    return (
        <div>
            {hashtags.map(()
                // TODO: mapping to html tag
            )

            }
        </div>

    )
}